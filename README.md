# 🛒 Опис роботи програми *Money-manager*

## 📌 Загальна логіка програми

Програма працює як простий **менеджер покупок**, який зберігає перелік товарів у вигляді списку словників.  
Користувач може:

- додавати нові покупки  
- переглядати наявний список  
- підраховувати загальну суму  
- зберігати список у файл  
- завантажувати дані з файлу  
- завершувати роботу програми  

Усе це організовано через консольне меню у функції `main()`.

---

## 🧩 Функції програми

### 1. `add_item(shopping_list)`
Ця функція відповідає за додавання нового товару до списку.  
Вона:

- отримує від користувача назву товару  
- запитує кількість  
- запитує ціну за одиницю  
- створює словник з цими даними  
- додає його до списку `shopping_list`  
- повідомляє користувача про успішне додавання  

---

### 2. `show_list(shopping_list)`
Функція відображає весь список покупок.  
Вона:

- перевіряє, чи список порожній  
- якщо ні — виводить кожен товар з його номером, назвою, кількістю і ціною  

---

### 3. `count_total(shopping_list)`
Функція підраховує повну суму всіх покупок.  
Вона:

- обчислює добуток *ціни × кількість* для кожного товару  
- додає всі значення  
- виводить загальну суму  

---

### 4. `save_to_file(shopping_list)`
Функція відповідає за збереження списку у текстовий файл.  
Вона:

- відкриває файл `text.txt` у режимі запису  
- проходить по всіх елементах списку  
- записує кожен товар у файл у текстовому форматі  

---

### 5. `load_from_file()`
Функція завантажує покупки із файлу.  
Вона:

- відкриває файл `text.txt` для читання  
- отримує повний текст із файлу  
- розділяє його на частини  
- формує з цих частин словники  
- додає їх у новий список покупок  
- повертає цей список  

---

### 6. `main()`
Це головний цикл програми.  
Він:

- виводить меню  
- запитує вибір користувача  
- залежно від вибору запускає відповідну функцію  
- дозволяє повторювати дії, поки користувач не обере вихід  

Кожна дія виконується у структурі `match`, що зручно для читання і підтримки меню.

---

## 📘 Результат

Таким чином програма складається з окремих функцій, кожна з яких відповідає за свою частину роботи менеджера покупок.  
Вона дозволяє додавати, переглядати, зберігати та завантажувати покупки, а також рахувати загальну суму витрат.
